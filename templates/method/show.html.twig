{% extends 'base.html.twig' %}

{% block title %}Method{% endblock %}

{% block body %}
    <h1>Method</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ method.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ method.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ method.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('method_index') }}">Back to list</a>

    <a href="{{ path('method_edit', {'id': method.id}) }}">Edit</a>

    {{ include('method/_delete_form.html.twig') }}
{% endblock %}
